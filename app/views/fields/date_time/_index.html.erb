<%# DateTime index partial for Administrate dashboard %>
<%# Override default partial to display time in user's local time %>

<% if field.data %>
  <%= field.data.in_time_zone(cookies[:timezone] || 'UTC').strftime('%b %d, %Y %l:%M%P') %>
<% end %>
